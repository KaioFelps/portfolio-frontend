import { env } from "$env/dynamic/private";
import type { PageServerLoad } from "./$types";

// export const load: PageServerLoad = (ctx) => {
//     const response = await ctx.fetch(`${env.BACKEND_URL}/tag/get`)
// };
